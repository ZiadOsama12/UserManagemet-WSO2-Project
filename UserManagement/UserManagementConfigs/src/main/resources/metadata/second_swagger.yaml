openapi: 3.0.1
info:
  title: second
  description: API Definition of second
  version: 1.0.0
servers:
- url: http://localhost:8290/second
paths:
  /test:
    get:
      responses:
        default:
          description: Default response
  /test22:
    put:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
